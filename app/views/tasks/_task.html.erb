<%= div_for task do %>

<strong><%= task.name %></strong> - <em>Opened on <%= task.created_at.strftime("%b %-d %Y") %></em> - <%= link_to "Close Task", close_task_path + "?task_id=" + task.id.to_s %>

<% end %>